# secursuit-e2e-vuln

Synthetic test repository created by the Secursuit production-readiness
E2E suite (persona-c). Contents are deliberately vulnerable — never run
the code inside, the dependencies cover known CVEs (lodash prototype
pollution, axios ReDoS, certifi compromise, etc).

Created: see commit timestamp. Auto-deleted at the end of the test run.
